{
  "name": "calculadora-ultrad-ha",
  "version": "1.0.1",
  "description": "Calculadora comercial ULTRAD® HA — Fermentech",
  "private": true,
  "scripts": {
    "start": "node scripts/servidor.js",
    "build": "node --test && node scripts/build.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
